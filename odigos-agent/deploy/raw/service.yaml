apiVersion: v1
kind: Service
metadata:
  name: odigos-ai-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: odigos-ai-agent
  ports:
    - name: http
      port: 8765
      targetPort: http
      protocol: TCP
