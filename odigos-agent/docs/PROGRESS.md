# Progress

One-line dated entries pointing to the chat that did the work.

- 2026-05-16 - Phase 0 scaffold (three containers, docker compose smoke test, graphify-out relocated under graph-mcp).
- 2026-05-16 - Phase 1a source / instrumentation MCP tools (8 reads + propose/apply_create_source approval pair). Shared `tools.BuildClients`, `ApprovalCache`. ADR-006/007/008.
- 2026-05-16 - Phase 1b collector MCP tools (7 tools incl. direct-HTTP `/metrics` scrape, regex log grep, parsed pipelines). ADR-009.
- 2026-05-16 - Phase 1c destination MCP tools (6 tools incl. masked secret inspection, gateway exporter cross-reference, direct TCP/TLS endpoint probe).
- 2026-05-16 - Phase 1d graph MCP tools (7 tools incl. graph_query/neighbors/path/community/god_nodes/list_communities/wiki_read) + gh_read_file citation with 30-min LRU. Phase 6 reminder added to PLAN.md for the deferred kind cluster smoke.
- 2026-05-16 - Phase 2 LangGraph diagnostic workflow: triage → {source, collector, destination} → synthesize. Per-subgraph ReAct loops with name-prefix tool partitioning + shared graph/wiki tools. Source subgraph proposes but never applies (apply lives in Phase 3 behind interrupt/approval). CLI gains `debug --namespace --kind --name` subcommand. ADR-010 through ADR-013.
- 2026-05-17 - Phase 4 deploy: standalone Helm chart + raw kustomize under `odigos-agent/deploy/`. Single pod with 3 sidecars (agent + mcp-cluster + mcp-graph), ClusterIP on 8765, TCP probes for the localhost-bound MCPs, HTTP probes for the FastAPI agent. RBAC narrowed - `get/list` on odigos.io CRDs (no `watch`, no informers in mcp/tools/*.go) + `create` on `sources` only, no `pods/exec`. ADR-014 (standalone chart) and ADR-015 (RBAC drops exec).
- 2026-05-17 - Phase 5 frontend integration: Go backend SSE proxy at `POST /api/ai/debug` + `POST /api/ai/approve/:session/:request_id` in `frontend/services/ai_agent.go` (env-driven `AI_AGENT_URL` / `AI_AGENT_TOKEN`, chunk-flushing pass-through). Webapp pieces under `frontend/webapp/`: typed SSE event union (`types/aiAgent.ts`), fetch+ReadableStream parser (`lib/aiAgent.ts`), `useAIDebug` reducer hook, floating "Fix with AI" button that appears when the ui-kit `SourceDrawer` is open, side-panel diagnostics view with phase-coloured step log, approval modal with 5-min countdown, and a final report card. Helm `ui.aiAgent.{url,tokenSecret}` injects env on `helm/odigos/templates/ui/deployment.yaml`. ADR-016.
- 2026-05-17 - Phase 6 validation scaffolding: `odigos-agent/deploy/test-scenarios/` with three reproducible failure-mode fixtures (`source-missing`, `destination-broken`, `collector-broken`) sharing a single busybox workload, each with `apply.sh` / `cleanup.sh` / `expected.json`; `validate.sh` runner that port-forwards `odigos-ai-agent`, drives `/debug` per scenario, and compares `report.root_cause` + `proposed_remediation` presence to expectations. Approval-flow exit criteria (Approve/Deny/timeout) documented as manual UI steps in `source-missing/README.md`. Structural test file `agent/tests/test_report_shape.py` (34 tests) locks down `Report` round-trip, `RootCause` / `Classification` / `RemediationStatus` literal sets, `ProposedRemediation.op` lockdown to `create_source`, the state-overrides-LLM remediation gating, findings ordering, and the full triage→routing table including the missing-triage fallback. Live kind-cluster runs + prompt tuning happen on top of this scaffold and land as a follow-up commit.
- 2026-05-17 - Phase 7 source instrumentation overrides: 3 new read MCP tools (`list_available_distros`, `get_workload_distro`, `check_obi_eligibility`) + 3 mutation pairs (`propose/apply_override_distro`, `propose/apply_enable_obi`, `propose/apply_disable_obi`) in `odigos-agent/mcp/tools/instrumentation.go`, all riding the existing `ApprovalCache` and reusing `FetchPodTemplate`. `PendingMutation` extended with `YAMLBefore`/`YAMLAfter`/`Context`. Distro registry lookup goes through `distros.NewCommunityGetter()` (added as a `replace`-directive module dependency in `mcp/go.mod`). `disable_obi` never deletes a rule - it rewrites `otelDistros.otelDistroNames` to the SDK default - so RBAC widens to `get/list/create/update/patch` on `instrumentationrules` (ADR-017) with no `delete`. Python `ProposedRemediation` now carries op union `create_source | override_distro | enable_obi | disable_obi` plus `yaml_before` / `yaml_after` / `context` fields; `_extract_proposed_remediation` walks for any propose_* tool, reads `op` from the payload, and skips unknown ops; `apply_remediation_node` looks the apply tool up by op; SSE `approval_required` event grows optional `yaml_before`/`yaml_after`/`context` (back-compat for `create_source`). SOURCE_SUBGRAPH_PROMPT gains a decision policy that picks the single best op per session. Frontend approval modal branches on `approval.op`: greenfield YAML for `create_source`, before/after stacked `<Pre>` blocks + a contextual callout for the other three ops. Two new validation scenarios: `wrong-distro` (Java workload pinned to the universal eBPF distro - expects `override_distro` back to `java-community`) and `obi-required` (C++ workload with a Source but no SDK distro registered for the language - expects `enable_obi` with the universal eBPF distro). `validate.sh` picks both up automatically. ADR-017.
